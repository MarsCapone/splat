function number natural (number list input) {
    function number accumulate (number acc) {
        function number last (number last) {
            if (lstend input) {
                0
            } else {
                let [a = (acc + (num (head input)))] {
                    let [print = showln (last + a)] {
                        natural #(tail input) #a #(last + a)
                    }
                }
            }
        }
    }
} #stdin #0 #0
