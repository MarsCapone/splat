<| Task 1 - Prints the input with one at start |>

function number t1 (string list splat) {
    if lstend splat {
        showln "endofstdin";
        (1+ 1)
    } else {
        let [h = (head splat)] {
            showln h;
            t1 #(tail splat)
        }
    }
} #stdin
