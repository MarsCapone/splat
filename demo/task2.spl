function list t2 (list splat) {
    if splat == [] {
        []
    } else {
        let [s = show (head splat)] {
            let [s = showln (head splat)] {
                t2 #(tail splat)
            }
        }
    }
} #stdin
