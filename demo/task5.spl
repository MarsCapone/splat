function number t5 (number first) {
    function number i1 (number second) {
        function number i2 (list splat) {
            if splat == [] {
                0
            } else {
                let [h = (as_num (head splat))] {
                    let [v = (h + first + second)] {
                        let [s = showln v] {
                            t5 #second #v #(tail splat)
                        }
                    }
                }
            }
        }
    }
} #0 #0 #stdin
