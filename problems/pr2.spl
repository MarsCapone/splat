<| Task 2 - Copying |>

function number t2 (list splat) {
    if splat == [] {
        0
    } else {
        let [s = showln( show (head splat))] {
            t2 #(tail splat)
        }
    }
} #stdin
